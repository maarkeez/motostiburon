# DataSource settings: set here configurations for the database connection
# spring.datasource.url = jdbc:mysql://localhost/alfred
# spring.datasource.username = alfred
# spring.datasource.password = 1234
# spring.datasource.driverClassName = com.mysql.jdbc.Driver

# Specify the DBMS
# spring.jpa.database = MYSQL

# Show or not log for each sql query
# spring.jpa.show-sql = true

# Hibernate settings are prefixed with spring.jpa.hibernate.*
# spring.jpa.hibernate.ddl-auto = none
# spring.jpa.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# spring.jpa.hibernate.naming_strategy = org.hibernate.cfg.ImprovedNamingStrategy

# logs the SQL statements
# log4j.logger.org.hibernate.SQL=debug 

# Logs the JDBC parameters passed to a query
# log4j.logger.org.hibernate.type=trace 


server.port = 8082

# Especificar limite de subida de adjuntos
multipart.maxFileSize=10Mb

# Poder realizar cambios en caliente.
spring.thymeleaf.cache=false

# Log de SPRING.
logging.level.org.springframework.web=DEBUG
logging.level.com.motostiburon=DEBUG

# Logs de tomcat embebido
server.tomcat.accessLogEnabled=true
server.tomcat.accessLogPattern=%a asdasd